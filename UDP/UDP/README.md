Basic message transport using UDP. Using DatagramSocket, DatagramPackets for this prototype.

Features:
1. Created UDP client and server.
2. Server runs indefinitely on a certain port waiting to listen the messages.
3. Client just sends a message and exits.
4. Server uses a buffer of 1024 bytes to store the incoming message.
6. Logging the messages.
